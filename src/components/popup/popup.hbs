<section class="popup popup_{{type}}">
   <div class="popup__content">
   <button type="button" class="popup__close" closePopup="{{type}}">
      <ion-icon name="add-outline" closePopup="{{type}}"></ion-icon>
   </button>
   <h2 class="popup__title">{{ title }}</h2>
      {{> @partial-block }}
   </div>
</section>